# Always take the LATEST akshare — it ships anti-scrape fixes in
# patch releases and we need those to stay ahead of Eastmoney/Sina
# blocks. Intentionally unpinned; see CLAUDE.md.
akshare

# Pin the infrastructure libs. Upstream breaks here would cascade
# silently (new pandas cutting deprecated APIs, new httpx changing
# connection-pool semantics, etc.).
httpx>=0.27,<1.0
pandas>=2.1,<3.1
pyarrow>=15.0,<25
